language: python
python:
- 3.6

before_install:
  - pip install codecov
  - pip install coverage

install:
  - pip install -r requirements.txt

script:
  - coverage run ./irobotclient/test/mock_irobot.py

after_success:
- codecov